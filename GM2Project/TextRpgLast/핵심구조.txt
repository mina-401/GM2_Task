1. TextRpgCore가 플레이어와 지역을 준비합니다.
   이때의 핵심함수는 TextRpgCore::Start(); =>에서 
   지역도 준비하고 플레이어도 준비시킨다.

   1-1
   플레이어 SetName으로 이름을 정해주고
   BeginPlay로 FightUnit를 준비시킨다.

   1-2.
   UWorld라는 클래스를 만들고
   그곳에서 모든 지역을 관리하고 있다.
   Uworld는 모든 지역을 만들고 연결을 담당한다.

   각 지역의 준비함수는 UWorld::ZoneInit() 실행된다.

   생성 => 준비 => 업데이트

2. 이제부터 모든 Actor BeginPlay를 실행시켜줘야 합니다.
3. BeginPlay의 의미는 진짜 사용하기 전에 능력치 랜더링
   등등에 필요한 요소들을 준비 해놓고 사용할수 단계로 간다.
   BeginPlay가 실행안되면 어떻게든 터질수 있다.
   
   
   
